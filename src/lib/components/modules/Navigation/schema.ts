import type {ISchema} from "../../component/ISchema";

export const schema: ISchema = {
    "name": "Navigation",
    "tag": "navigation",
    "class": "w-full",
    "settings": [
        {
            "type": "text",
            "id": "title",
            "label": "Title Text",
            "default": "AGE AUTHORIZATION"
        }
    ]
}